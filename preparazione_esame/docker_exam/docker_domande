1. Creazione registro custom per immagini docker:
utilizzo docker hub / docker registry create -> container (immagine dedicata docker)

2. creazione account -> login -> docker push

3. percorso immagine compresa di registro sorgente e versione
registro/immagine:versione

4. nome repo docker hub corrisponde a:
nome repo = nome immagine

5. come si chiama metodo compilazione degli ultimi esercizi: multi-stage

6. argomenti vs variabili ambiente -> arg validi fino alla build, variabili ambiente dopo build container

7. RUN vs EXEC -> ?

8. volumi, condivisione risorsa con volumi: -v (path cartella o nome volume)

9. creazione volume -> docker volume create (volume = cartella condivisa tra sistema host e container)

10. altri tipologie di volumi: econfig, secrets ?

11. accesso secrets: /run/secrets/ ?

12. creazione config: docker config ?

13. base64 codifica immagini, testo, eseguibili

14. codifica/decodifica testo base64 ?

15. cluster: cos'è (insieme di nodi che comunicano tra loro e isolati tra loro), visto come un sistema a se/una cosa singola

16. cluster docker: manager/worker, minimo nodi: 1

17. docker swarm init (--advertise addr=specifico IP nel caso in cui il nodo di partenza abbia piu interfaccie di rete) -> ottengo un token per aggiunta nodi

18. master vs worker: gestione worker, un master può far girare i container del cluster

19. nodi master terminati: cluster immutabile -> servizio funzionante; non posso aggiungere nodi/cambiare label

20. numero nodi master -> numero nodi -1 / 2 -> con 3 nodi tollero la morte di 1 nodo (2 master e 1 worker) ?

21. elezione manager -> algoritmo di RAFT

22. cluster + nodo locale -> come mettere in comunicazione? -> creazione context per mettere in comunicazione 

23. sistema autenticazione su un processo docker in remoto -> utilizzo certificati (mutua autenticazione)

24. cosa devo fare per configurare demone docker per rispondere a subnet, porta, destinazione (lato demone docker) -> daemon.json o edito processo systemd

25. daemon.json va creato, non ce di default

26. gestione e orchestrazione più container -> compose (se sono su un cluster docker stack)

27. compose -> services (più istanze = repliche), cluster -> ?

28. gestione repliche (mode: global)?

29. come vedo log container -> docker logs -f nomeContainer

30. cosa posso impostare sui log -> dimensione massima, driver da utilizzare per inizializzare i log

31. limitazione in CPU e RAM -> core e MB

32. limite inferiore -> reservation  limite superiore -> limits

33. reservation -> preallocazione risorsa    limits -> ?

34. se non preallocata la risorsa non parte il container

35. ruolo orchestratore -> ?